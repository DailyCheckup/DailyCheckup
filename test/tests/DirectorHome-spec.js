import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';
import DirectorHome from './../../src/Components/DirectorHome.jsx';
import QuizTakenTable from './../../src/Graphs/QuizTakenList.jsx';

describe('Director Home', () => {
  // const testState = {
  //   takenQuizData: [['Name', 'Taken Quiz', 'No. Correct'],['Dorothy Zbornak', true, 4],['Rose Nylund', true, 1],['Blanche Devereaux', false, 0]],
  // };
  // const wrapper = mount(<DirectorHome directorState={testState} />);
  // it('will render', () => {
  //   expect(wrapper.find('div')).to.have.length(1);
  // });
});
